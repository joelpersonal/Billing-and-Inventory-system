# 🔧 Billfinity Backend Environment Configuration
# Copy this file to .env and fill in your actual values

# 🗄️ DATABASE CONFIGURATION
# MongoDB Atlas connection string (required)
# Get this from: https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/billfinity?retryWrites=true&w=majority

# 🔐 AUTHENTICATION
# JWT secret key for token signing (required)
# Generate a secure random string (minimum 32 characters)
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long

# 🌐 SERVER CONFIGURATION
# Port for the backend server (optional, defaults to 5001)
PORT=5001

# Environment mode (development/production)
NODE_ENV=development

# Frontend URL for CORS (optional, auto-detected in production)
FRONTEND_URL=http://localhost:5173

# 📧 EMAIL CONFIGURATION (Optional - for invoice emails)
# Gmail SMTP settings
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-gmail-app-password

# 🤖 AI SERVICES (Optional - for AI search features)
# Hugging Face API key (free tier available)
# Get from: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=hf_your_hugging_face_api_key_here

# 📊 ANALYTICS (Optional)
# Add any analytics or monitoring service keys here
# ANALYTICS_KEY=your_analytics_key

# 🔧 DEVELOPMENT SETTINGS
# Enable debug logging in development
DEBUG=billfinity:*

# 📝 NOTES:
# 1. Never commit the actual .env file to version control
# 2. Use strong, unique passwords and secrets
# 3. For production, use environment variables in your hosting platform
# 4. MongoDB Atlas M0 (free tier) is sufficient for development and small production loads
# 5. Hugging Face free tier provides 1000 API calls per month